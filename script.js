const button1 = document.getElementById('Sød kartoffel');

// Your condition (true or false)
const condition = true; // Change this to your desired condition

// Change the button color based on the condition
if (condition) {
  button.style.backgroundColor = 'green'; // Change to your desired color
} else {
  button.style.backgroundColor = 'red'; // Change to your desired color
}